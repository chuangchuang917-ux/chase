# 臺灣 50 成分股歷史籌碼下載與篩選調整任務

- `[x]` 1. 日籌碼資料爬取：已爬取台灣 50 指數 50 檔成分股自 2025-12-01 至 2026-06-22 共 6,600 筆日籌碼明細。
- `[x]` 2. 策略邏輯調整：已修改 `strategy.py` 中的週集保大戶過濾邏輯，僅評估 `holder_over_1000` (千張大戶比) 連續上升，不再驗證散戶持股比與總股東人數。
- `[x]` 3. 集保所 Norway 爬蟲開發：開發 `scrape_weekly_norway.py` 腳本，將 `holder_under_50` 替換為 `holder_over_400`。
- `[x]` 4. 爬蟲網頁解析 Bug 修復：解決了 `norway.twsthr.info` 表格結構解析時 nested table 導致 empty cols 以及 Sidebar Table 誤判的 Bug。使用 `recursive=False` 精確匹配 Table 9，並以台積電（2330）驗證成功。
- `[x]` 5. 執行全量成分股集保資料下載：執行 `python scrape_weekly_norway.py` 爬取全量 50 檔個股 120 天歷史週集保資料，已爬取 1,400 筆紀錄寫入資料庫。
- `[x]` 6. Streamlit 儀表板數據呈現驗證：啟動並驗證 Streamlit 儀表板正常運作且 `400張以上大戶比` 欄位與指標皆正確展示。
- `[x]` 7. 移除「50張以下散戶比」與「總股東人數」欄位：已從資料庫結構、爬蟲程式、策略運算和 Streamlit 儀表板中全面刪除該欄位及其展示，並成功重跑爬蟲填充乾淨歷史數據。
- `[x]` 8. 交接文檔撰寫：已將「台股全市場籌碼雷達升級指南（排除集中度）」完整編寫入 `walkthrough.md`，方便下一個 Agent 接手開發。
- `[x]` 9. 移除「本頁均價」KPI 卡片：已將「本頁均價」指標卡片從 Streamlit 介面中移除，並調整為 3 欄對稱排版，美化整體介面。
- `[x]` 10. 移除主表格中的「💎 長線鎖碼」與「🔥 買盤加速」欄位：在 `app.py` 中將此兩列從主資料表格中徹底移除。
- `[x]` 11. 新增大戶持股連續增減週數提醒：在「集保大戶結構」的「千張大戶持股比」與「400張以上大戶比」指標卡片下新增 delta 連續上升/下降週數提醒，提供清晰的趨勢判讀。
- `[x]` 12. 移除「主力集中度滾動趨勢折線圖」及其老手解讀指南：已在 `app.py` 中將該圖表與說明區塊完全刪除，使「庫存籌碼監控警報器」直接銜接在「信用交易」區塊下方。
- `[x]` 13. 新增「今日」重設日期按鈕：在側邊欄的「選擇策略分析日期」旁新增一個「今日」按鈕，點擊後會自動重置為最新交易日期。
- `[x]` 14. 標示收盤價特定日期：在「當日收盤價」指標標題中動態嵌入所選日期 (例如：`收盤價 (2026/06/22)`)，以方便使用者清楚判讀價格所屬日期。
- `[x]` 15. 欄位顯示/隱藏與順序持久化機制重構：在 `app.py` 中將 `load_column_order` 與 `save_column_order` 改為支援字典格式的 `load_column_config` 與 `save_column_config`，並在 session state 中維持獨立狀態。
- `[x]` 16. 主表格渲染欄位過濾：在 `app.py` 中更新主表格僅顯示未隱藏之欄位。
- `[x]` 17. 側邊欄自訂欄位控制項升級：在 `app.py` 中更新多選欄位控制逻辑，並加入 🔼 🔽 自訂排序微調按鈕。
- `[x]` 18. 功能驗證與測試：在瀏覽器中驗證欄位隱藏、排序與重新整理時的狀態持久化。
- `[x]` 19. 停止執行中的 Streamlit 伺服器 (任務 1577)
- `[x]` 20. 將所有籌碼雷達程式、資料與暫存檔移入 `chase` 子資料夾
- `[x]` 21. 在 `chase` 資料夾中重啟 Streamlit 伺服器
- `[x]` 22. 驗證遷移後的系統功能是否完好如初（包含全市場日報與週集保資料爬取驗證、快取載入效能以及非 T50 個股數據如中鋼 2002 渲染）
- `[x]` 23. 歷史信用交易補件：補齊其餘 383 檔股票之融資券數據
- `[x]` 24. 抽樣數據比對：隨機抽樣 100 檔股票與 FinMind API 過去 120 天數據進行核對
- `[x]` 25. 上傳 SQLite：將最新完整數據之 taiwan_stock.db 上傳至 GitHub 遠端
- `[x]` 26. 對接 Supabase：在 app.py 中實作 Supabase 雲端資料庫雙軌讀取與週資料 resampling 邏輯
- `[x]` 27. 執行 `backfill_tpex_finmind.py` 補齊最後 302 檔上櫃股的歷史籌碼與信用交易資料 (全部 302 檔已完成)
- `[x]` 28. 執行 `sync_to_supabase_bulk.py` 重新計算策略指標並全量同步至 Supabase
- `[x]` 29. 驗證線上網頁歷史數據顯示與指標計算正確 (已驗證 Supabase 中 9950, 9951, 9960, 9962 的指標正確無誤)
- `[x]` 30. 更新 `walkthrough.md` 記錄任務完成與狀態
- `[x]` 31. 快取最佳化：將快取 TTL 從 3600 秒縮短至 300 秒，以加速每日同步的交易日期更新。
- `[x]` 32. 法人買賣天數指標：20日法人佔量比新增連續買賣天數 (Delta)，連買顯示紅色，連賣顯示綠色；移除60日漲跌幅的 Delta。
- `[x]` 33. Supabase RLS 安全漏洞修復與金鑰環境變數載入：修復 LOHAS 的 RLS 警告，並重構 Chase 同步腳本，將 `service_role` 私鑰寫入本地 Git 忽略的 `.env` 檔案，本地測試執行成功。
- `[x]` 34. 修正 06-30 資料不完整與大戶比為 0 的問題：更新了 crawler.py 的逾期 Token，在 daily_update.py 中整合週資料爬蟲以防大戶比歸零，並重新爬取、同步了 06-30 全市場 1,971 筆資料至 Supabase。
- `[x]` 35. 擺脫 FinMind 依賴：重構 crawler.py 中獲取活躍股票清單的邏輯，改為直接對接官方（TWSE/TPEx）開放資料，配合 SQLite 近期活躍資料備用，實現 100% 免 Token 的每日自動增量更新。



